# coding: utf-8

"""
Unikraft Cloud Platform

This is the API of the Unikraft Cloud Platform.

The version of the OpenAPI document: 0.6.0
Contact: info@unikraft.cloud
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from unikraft_cloud_platform.models.volume_attached_to_inner import VolumeAttachedToInner
from unikraft_cloud_platform.models.volume_mounted_by_inner import VolumeMountedByInner
from typing import Optional, Set
from typing_extensions import Self


class Volume(BaseModel):
    """
    Volume
    """  # noqa: E501

    status: Optional[StrictStr] = Field(
        default=None, description="The status of the response.  Note: Not when listing all existing volumes. "
    )
    state: Optional[StrictStr] = Field(
        default=None,
        description="A volume can be in one of the following states:  State           | Description ----------------|--- `uninitialized` | The volume is scheduled to be created. `initialized`   | The volume is currently created and formatted. `available`     | The volume is healthy and available to be attached to an instance. `idle`          | The volume is healthy and attached to an instance. It is possible to [detach](https://unikraft.cloud/docs/api/v1/volumes/#detach) it in this state. `mounted`       | The volume is currently mounted in an instance. `busy`          | There are maintenance tasks running on the volume. `error`         | The volume is in an error state that needs inspection by a Unikraft Cloud engineer. ",
    )
    uuid: Optional[UUID] = Field(default=None, description="The UUID of the volume. ")
    name: Optional[Annotated[str, Field(strict=True, max_length=255)]] = Field(
        default=None, description="The name of the volume. "
    )
    size_mb: Optional[Annotated[int, Field(le=1048576, strict=True, ge=1)]] = Field(
        default=None, description="Size of the volume in megabytes. "
    )
    attached_to: Optional[Annotated[List[VolumeAttachedToInner], Field(max_length=253)]] = Field(
        default=None, description="List of instances that this volume is attached to. "
    )
    mounted_by: Optional[Annotated[List[VolumeMountedByInner], Field(max_length=253)]] = Field(
        default=None, description="List of instances that have this volume mounted. "
    )
    persistent: Optional[StrictBool] = Field(
        default=None,
        description="Indicates if the volume will stay alive when the last instance is deleted that this volume is attached to. ",
    )
    created_at: Optional[datetime] = Field(default=None, description="Date and time of creation in ISO8601. ")
    __properties: ClassVar[List[str]] = [
        "status",
        "state",
        "uuid",
        "name",
        "size_mb",
        "attached_to",
        "mounted_by",
        "persistent",
        "created_at",
    ]

    @field_validator("status")
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["success", "error"]):
            raise ValueError("must be one of enum values ('success', 'error')")
        return value

    @field_validator("state")
    def state_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["uninitialized", "initialized", "available", "idle", "mounted", "busy", "error"]):
            raise ValueError(
                "must be one of enum values ('uninitialized', 'initialized', 'available', 'idle', 'mounted', 'busy', 'error')"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Volume from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in attached_to (list)
        _items = []
        if self.attached_to:
            for _item_attached_to in self.attached_to:
                if _item_attached_to:
                    _items.append(_item_attached_to.to_dict())
            _dict["attached_to"] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in mounted_by (list)
        _items = []
        if self.mounted_by:
            for _item_mounted_by in self.mounted_by:
                if _item_mounted_by:
                    _items.append(_item_mounted_by.to_dict())
            _dict["mounted_by"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Volume from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "status": obj.get("status"),
                "state": obj.get("state"),
                "uuid": obj.get("uuid"),
                "name": obj.get("name"),
                "size_mb": obj.get("size_mb"),
                "attached_to": [VolumeAttachedToInner.from_dict(_item) for _item in obj["attached_to"]]
                if obj.get("attached_to") is not None
                else None,
                "mounted_by": [VolumeMountedByInner.from_dict(_item) for _item in obj["mounted_by"]]
                if obj.get("mounted_by") is not None
                else None,
                "persistent": obj.get("persistent"),
                "created_at": obj.get("created_at"),
            }
        )
        return _obj
